[build]
builder = "RAILPACK"
buildCommand = "bun run build"
watchPatterns = ["src/**", "public/**", "vite.config.ts", "package.json", "bun.lock"]

[deploy]
startCommand = "bun run start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
